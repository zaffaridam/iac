- hosts: terraform-ansible
  tasks:
    - name: "Create index.html file"
      copy:
        dest: /home/ubuntu/index.html
        content: "<h1>Feito com terraform-ansible</h1>"
    - name: "Deploy httpd server"
      shell: "nohup busdybox httpd -f -p 8080 &"
